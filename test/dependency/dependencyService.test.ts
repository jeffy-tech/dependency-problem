import * as dependencyService from "../../src/dependency/dependencyService"

describe('dependencyService', () => {
    //

    it("should return the dependency name and version", () => {
        
    })
})